#' Generate Cytoscape sif
#'
#' @param fgnem Results struct from FGNEM. Needs to contain a [-1,0,1] nxn matrix `adj` and list `egene.logprobs`` of 3xm matrices
#' @param sif.file the file to write to
fgnemToCytoscape = function( fgnem, sif.file ){
  # TODO
}
